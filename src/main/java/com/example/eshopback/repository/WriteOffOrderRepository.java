package com.example.eshopback.repository;

import com.example.eshopback.model.entity.WriteOffOrder;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WriteOffOrderRepository extends JpaRepository<WriteOffOrder, Long> {
}
